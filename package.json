{
  "name": "apple-mcp-server",
  "version": "1.0.0",
  "description": "Apple MCP Server — macOS system controls as MCP tools for Claude",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "apple-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}